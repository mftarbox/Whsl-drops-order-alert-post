# Design spec: Wholesale WIP "Dropped" → NetSuite Order Rework Alert

Full history of decisions and platform findings from building this automation (2026-08-07 build session). Kept here as the record of *why* the code looks the way it does.

## Goal

When a style's Planning Indicator moves to Dropped on Wholesale WIP, find every not-yet-fulfilled NetSuite wholesale order with a line item matching that style's Master SKU, and create/update a Monday pulse per affected order on Wholesale Sales L10 → Customer Drops Communication Needed.

## Decisions

1. Trigger read: WIP2027's real `status_1__1` field, not Wholesale WIP's mirror column - monday.com's API cannot read or filter "lookup" (mirror) column types at all (confirmed directly: `"This column type is not supported yet in the API"`). Wholesale WIP's `board_relation_mm5z9qmh` link is used to scope WIP2027's 1,776 items down to the ~20 wholesale-relevant ones.
2. Delivery: scheduled GitHub Actions polling, not a live Monday webhook.
3. Cadence: once daily at 8:00 PM Mountain time. GitHub Actions cron is fixed UTC and doesn't observe daylight saving, so the workflow schedules two cron ticks (covering both MST and MDT) and the script itself checks the current Mountain hour, no-opping on whichever tick isn't actually 8pm right now.
4. Wholesale identifier: NetSuite `cseg_order_class` (a **custom** "Order Class" segment field, distinct from the standard NetSuite `class` field, which also exists on transactions but isn't the one used) = internal ID 7, confirmed via `BUILTIN.DF(cseg_order_class)` → "Wholesale".
5. "Not fulfilled" (header): order status B (Pending Fulfillment), D (Partially Fulfilled), or E (Pending Billing/Partially Fulfilled).
6. "Not fulfilled" (line level): required - a partially-fulfilled order should only trigger if the *specific line* matching the dropped SKU still has remaining quantity. `quantityFulfilled` isn't exposed to NetSuite's Search/SuiteQL API at all, so remaining quantity is computed via NetSuite's standard `PreviousTransactionLineLink` table (`linktype = 'ShipRcpt'`) joined back to the shipped Item Fulfillment lines, summed and compared to `quantity` (which is stored negative for outbound transactions like Sales Orders - use `ABS()`).
7. Destination: Wholesale Sales L10 (board 3552781534), group "Customer Drops Communication Needed" (`group_mm1mrkqe`). (Changed 2026-08-21, Shelly's request - originally group "Wholesale Order Board" (`new_group_mkn8x1ye`); only affects `groupId` on newly-created pulses going forward, existing pulses already sitting in the old group are untouched.)
8. Idempotency: checkbox column "Rework Alert Sent" (`boolean_mm60zwwz`) added to Wholesale WIP - only unprocessed + Dropped items are considered.
9. No backfill - only styles that move to Dropped after this automation went live. (As of go-live, none of the 20 wholesale-relevant styles were sitting at Dropped, so no manual pre-check of the new checkbox was needed.)
10. Pulse name: `"{Order #} has drops - {Customer Name}"`.
11. Pulse `person`: order's `salesrep` (an employee) → if that employee's `custentity_shin_inside_rep` is true, use the salesrep; if false, use that same employee's `custentity_assigned_inside_rep` instead. Both fields live on the **Employee** record, not the Customer record (an early assumption that was wrong - customer-record fetches for these field IDs silently returned nothing rather than erroring, which is what surfaced the mistake). The resolved employee is matched to a Monday user by email (`users(emails: [...])`).
12. All other pulse columns (Priority, Status, Due Date, Notes, External ID) intentionally left blank for now.
13. An order that later matches a second dropped SKU gets an update/comment appended to its existing pulse rather than a duplicate pulse, tracked via `state/state.json` (order # → Monday pulse ID), committed back to the repo by the workflow each run.
14. No additional notification channel - the Monday pulse itself is sufficient.
15. Before appending an update to a previously-created pulse (via `state/state.json`), the script checks that pulse's current `state` on monday (`active`, `archived`, `deleted`, or not found at all for a fully-deleted item). Only `active` pulses get the append. If the pulse was archived, trashed/deleted, or no longer exists, the script creates a brand-new pulse instead and overwrites the order's entry in `state/state.json` to point to the new pulse ID (added 2026-08-08, per Shelly's direction after the Monday OAuth app rebuild).
16. Pulse comment: every pulse - both on first creation and on every later append - gets a comment containing the NetSuite sales order link, customer name, order number, a "Master SKU | Shopify Title" line per distinct dropped style matched on that order, and a "Variant Size | Qty Ordered" line per matched order line, sorted by `custitem42` (position, ascending; items missing a position sort last). Field sources confirmed with Shelly directly against a real item record: Shopify title = `custitem_nu_sales_title`, variant size = `custitem35`. NetSuite order link format confirmed from Shelly's own browser: `https://{NETSUITE_ACCOUNT_ID}.app.netsuite.com/app/accounting/transactions/salesord.nl?id={so_internal_id}` (the classic UI domain - different from the SuiteTalk REST domain used for API calls). Scope is "this run's drops only," not a full cumulative history - if two styles drop in the same run and both match the same order, they're combined into one comment; drops from earlier runs already appended their own separate comments and aren't re-listed. (This was briefly reverted on 2026-08-07/08 while a missing `updates:write` OAuth scope on the Monday app was tracked down - see the OAuth troubleshooting note in "Platform quirks" below - and reinstated once the app was rebuilt with the correct scope and confirmed working live on 2026-08-08.)

17. **Add to NuOrder -> Remove** (built 2026-08-08): unconditional side effect of a style dropping, fires at the very start of that item's processing loop - before order matching, before the Wholesale L10 / Sales Ops L10 pulses. Sets Wholesale WIP's `color_mm5zz20q` ("Add to NuOrder", a status column with labels Add/Remove/Synced) to "Remove". Wrapped in its own try/catch so a failure here doesn't block anything else for that item. The existing Celigo integration automatically unchecks NetSuite's "NuOrder Active" checkbox (NOT "NuOrder Sync") on the matching item(s) once this flips - nothing built for that half, it already happens on its own. "Rework Alert Sent" semantics are unchanged - Shelly explicitly rejected making it conditional on every other step succeeding, due to duplicate-comment risk on partial retries.

18. **Sales Ops L10 "close items" CSV pulse** (built 2026-08-08): same Dropped trigger, same run, reuses the exact rows already fetched by the Wholesale L10 order-matching query (no second SuiteQL query) - one CSV per dropped *style* (not per order), aggregating every matched line across every matched order for that style. Columns (as of 2026-08-10, see 18a/18b - Amount/Item were added and then dropped again same day): **Internal ID** (`so.id`), **Closed** (always literal `TRUE`), **Line ID** (the simple `line` sequence number - see 18b for why NOT `lineUniqueKey`). `line` comes from `getSalesOrderLines()` in `lib/netsuite.js`, a `GET /record/v1/salesorder/{id}?expandSubResources=true` call - confirmed live against a real order (MSO-4061, internal id 62186518) that this returns `item.items[]`, each entry shaped like `{ item: { id, refName }, line, lineUniqueKey, isClosed, amount, ... }`. Since the SuiteQL matching query can return multiple rows for the same item on one order (e.g. the same SKU ordered on two separate lines), matched lines are handed out from a per-order, per-item-id queue (`resolveOrderLineDetails`) so two SuiteQL rows can never both grab the same underlying NetSuite line. Destination: **Sales Ops L10** (7017226460), group **ToDo** (`new_group72329__1`), pulse named `"{Master SKU} dropped - Close items on Sales Orders"`, Priority Level (`status_1_mkm2z4qr`) = **"High Priority"**. One comment contains both a hyperlink (display text **"Whsl Close Sales Order Items"**) to the Close Sales Order Items CSV Import mapping (`https://4775967.app.netsuite.com/app/setup/assistants/nsimport/importassistant.nl?recid=311&new=T`) and the CSV attached directly to that comment via `add_file_to_update` (Sales Ops L10 has no Files-type column, confirmed fine with Shelly).

18a. **Close items CSV: Closed always TRUE; Amount/Item added, then dropped again** (2026-08-10): Shelly first asked for `Closed` to always write literal `TRUE` (every line on this CSV is one being closed, regardless of its actual `isClosed` value in NetSuite - all-caps per her explicit choice over lowercase `true`) - that stuck. She also asked for an `Amount` column (raw NetSuite line `amount`, as-is) and an `Item` column (the line's `item.refName`, already `Parent : child-size`) to be added for Sales Ops visibility; both were informational only, since the saved import (recid=311) only ever mapped Internal ID/Closed/Line ID to real NetSuite fields. After previewing that 5-column version, Shelly asked to drop Amount and Item again - final column order is back to **Internal ID, Closed, Line ID** (3 columns, matching decision 18's current state). `buildCloseItemsCsv()` and the `csvRows.push()` call site in `run.js` were trimmed accordingly; `getSalesOrderLines()` still returns `amount`/`itemRefName` (cheap off the same fetch) but they're unused for now.

18b. **Line ID: resolved to the simple `line` sequence number, NOT lineUniqueKey** (2026-08-10): Preview CSVs were built and approved against a real order (WSO-DropTEST, internal id 62270327) using `lineUniqueKey` for "Line ID", per an earlier verbal confirmation from Shelly (recorded in decision 18's original text) that "Line ID" meant lineUniqueKey - a persistent, position-independent identifier that stays valid even if the order is resaved or lines are reordered, which is Shelly's hard requirement for this feature. When Shelly manually tested one real line (lineUniqueKey 251186457) via the saved "Close Sales Order Items" import, it failed with "The record has been deleted since you retrieved it." Screenshots of the import's Field Mapping step showed "Line ID" was actually bound to NetSuite's `line` field (the simple, position-dependent sequence number), not `lineuniquekey` - sending a lineUniqueKey value like `251186457` there made NetSuite try to resolve it as line position 251186457 on a much-shorter order, which doesn't exist. Research (Oracle's own CSV import docs, and independent NetSuite consultant write-ups) confirmed this is a real platform gap, not a configuration mistake: the classic CSV Import Assistant's transaction-line matching only supports the positional `line` field ("Order Line"/"Line Id") - `lineuniquekey` is a newer field exposed via NetSuite's REST/SuiteScript APIs but was never added to the classic Import Assistant's mappable-field list for transaction sublists, confirmed by Shelly finding no lineuniquekey option in the recid=311 mapping dropdown. Shelly then manually re-tested using the correct `line` value (2, matching lineUniqueKey 251186457's actual current position) via the exact 3-field mapping shown in her screenshot (Internal ID -> `id`, Closed -> `isclosed`, Line ID -> `line`) and confirmed it works - a follow-up live fetch of order 62270327 shows that line now has `isClosed: true`. **Decision: use `line`, not `lineUniqueKey`, for the CSV's "Line ID" column** - `buildCloseItemsCsv()` and `getSalesOrderLines()` in `lib/netsuite.js` were updated accordingly. **Known limitation, accepted by Shelly**: since `line` is position-dependent, this CSV's Line ID values are only valid as of the moment the CSV was generated - if the order is resaved with lines reordered/added/removed before the CSV is imported, the wrong line could get closed. Mitigate by importing promptly after the CSV is posted to Sales Ops L10.

19. **NuOrder imagery export to Dropbox** (built 2026-08-08): a wholly independent trigger from Dropped - runs every time this script runs (same 8pm Mountain cadence), gated on Wholesale WIP's real `color_mm5zz20q` ("Add to NuOrder") = "Add" and WIP2027's own `boolean_mm61mp3r` ("NuOrder Images Exported") checkbox being unchecked. This idempotency-checkbox design deliberately replaces needing to track "did this just change to Add" against a prior poll - it naturally covers both "newly changed to Add" and "new item created already at Add," and the WIP2027-side "New Pic!" button re-triggers a re-export simply by unchecking the same box. **Monday-side setup for this feature (columns + automation) lives on WIP2027 (18388071004), not Wholesale WIP** - see the "Option B" note below for why. Images come from WIP2027's real `files2__1` field (Wholesale WIP's own Image column is also an unreadable mirror, same limitation as Planning Indicator). Export filename = `{Master SKU}-0{sequence}.{ext}` (e.g. `10176-3232-01.png`) where sequence is a literal single digit (1-5) in the order files appear in the column (upload order) - confirmed with Shelly against real WIP2027 filenames, which do NOT already match this convention and must be renamed on export, not passed through. Binary content/type/size pass through completely untouched. Upload is to Dropbox path `/NuOrder Imagery/{filename}` in **overwrite** mode (same filename replaces in place, no auto-renamed duplicate).

20a. **Add to NuOrder -> "Update NetSuite" after imagery export** (built 2026-08-08): added after the base feature 3 build and confirmed live-working. After each item's export attempt in `processNuOrderImageExports` - success or failure - Wholesale WIP's `color_mm5zz20q` gets set to its **"Update NetSuite"** label (confirmed via `get_board_info` this is a real, existing label on that column, not a typo for "Synced" - the column's own Monday description says it "drives the Celigo sync that checks/unchecks the NuOrder Active / NuORDER Sync boxes on the matching NetSuite item(s)"). Shelly explicitly chose "fire regardless" over "only on full success" when asked - implemented via a `finally` block wrapped in its own independent try/catch (so a failure flipping the status gets its own Slack alert rather than masking, or being masked by, the export result). Once flipped away from "Add," the item naturally drops out of this feature's `targets` filter on the next run, so it won't re-fire repeatedly.

20. **Slack error alerts** (built 2026-08-08): a Slack Incoming Webhook (`SLACK_ALERTS_WEBHOOK_URL`, no bot/OAuth scopes needed) posts to **#whsl_ops_workflow_alerts** (C0ARSBBPGP7) on **both** fatal (uncaught, whole-run-ending, via the top-level `main().catch()`) and per-item/per-order errors that are otherwise just logged and swallowed (NetSuite query failures, item/customer detail lookups, pulse creation failures, the NuOrder flag flip, the imagery export, etc.) - confirmed by Shelly: alert on both tiers. `sendSlackAlert()` in `lib/slack.js` is deliberately built to never throw, so a Slack outage or bad webhook URL can't itself take down the real automation run. A related ask - manual workflow triggers *from* Slack (full-run and imagery-only) - was scoped in the same conversation but explicitly paused by Shelly as more work than she wants to take on right now; see project memory for that scope if it gets picked back up later.

21. **Trigger Celigo's native "Monday to NetSuite" sync flow in-workflow instead of on its own separate schedule** (built 2026-08-10): Shelly's existing Celigo flow watches for "Add to NuOrder" changes (both the "Remove" label from feature 1 and the "Update NetSuite" label from 20a) and syncs the NuOrder Active/Sync checkboxes to NetSuite - previously purely on its own native Celigo schedule, independent of this script. Shelly asked to trigger that flow from within this run instead, fully replacing its separate schedule. **Timing decision**: within one run, "Add to NuOrder" can change in two places - the Update NetSuite flip (inside `processNuOrderImageExports`, runs first) and the Remove flip (inside the per-dropped-item loop, runs later). Rather than call Celigo inline at each of those two spots (which could fire the API multiple times per run and would need two separate call sites), Shelly chose a single call once at the very end of the run (`finishRun()`), after every possible "Add to NuOrder" change for that run has already happened - including on the early-exit path where there were no drop candidates, since feature 3's flips still happen before that check. Fires unconditionally every run (not gated on whether anything actually changed - Shelly's call), and never throws past its own try/catch - a Celigo failure logs + Slack-alerts like every other non-fatal error path here, but doesn't affect the run's exit code (Shelly's call, matching this script's existing conventions).
    - **Mechanism**: Celigo's `POST https://api.integrator.io/v1/flows/{flowId}/run` runs a flow on-demand independent of its native schedule - confirmed via Celigo's own API docs that a flow doesn't need reconfiguring to support this (the schedule and on-demand-run capability are separate settings).
    - **Flow ID**: `6a7520cb8d2dd2212253f91d` (from the flow's URL in integrator.io: `https://integrator.io/integrations/66f5896552a1b5dd437da348/flowBuilder/6a7520cb8d2dd2212253f91d`), hardcoded as `CELIGO_MONDAY_TO_NETSUITE_FLOW_ID` in `run.js` (not a secret - just an identifier, same convention as the Monday board/column IDs elsewhere in this file).
    - **Credential quirk worth remembering**: Shelly's Celigo login has access via a **shared account**, not an owned one. Celigo's UI rejected setting *any* access-scope field on a generated token - including just toggling "Full access" on - with the error "Scope fields cannot be set on shared account tokens. Permissions are inherited from the account share." The fix: generate the token with only name/description/expiration set and no scope fields touched at all (not even an empty selection) - permissions are then inherited automatically from whatever the account share already grants. If this token ever needs broader/different access than the account share provides, that has to be changed by whoever owns/administers the parent Celigo account - not fixable from Shelly's own token settings.
    - Implemented in `lib/celigo.js` (`runCeligoFlow(flowId)`), following the same minimal-fetch-client pattern as `lib/dropbox.js`/`lib/monday.js`. Token lives in the `CELIGO_API_TOKEN` GitHub secret.
    - **Bug found and fixed (2026-08-21) before `CELIGO_API_TOKEN` was even added**: the first manual test run crashed the *entire* script (not just the Celigo step) with `Missing required environment variable: CELIGO_API_TOKEN`, because `lib/celigo.js` originally checked for the token at module load time (`const API_TOKEN = requireEnv(...)` at the top of the file) - same pattern as `lib/monday.js`/`lib/dropbox.js`, which is correct for THOSE since they're hard requirements for the whole script, but wrong here since Celigo failures are supposed to log + Slack-alert without affecting anything else (per the original design in this same decision). Fixed by moving the `requireEnv('CELIGO_API_TOKEN')` call inside `runCeligoFlow()` itself, so it only throws when the function is actually called - which `finishRun()`'s try/catch already handles correctly.
    - **Confirmed working live 2026-08-21**: after also adding `CELIGO_API_TOKEN` to the workflow's `env:` block in `daily-check.yml` (a separate miss - GitHub secrets aren't automatically injected into a script's environment, they have to be explicitly listed there), a real run logged `Triggered Celigo Monday-to-NetSuite sync flow.` with no error. Still worth checking Celigo's own flow run history at some point to confirm the flow completes on their end, not just that the trigger call was accepted.

22. **Fixed a real Monday API pagination gap - up to 238 items were invisible to this script every run** (found and fixed 2026-08-21): Shelly reported a real "Dropped" style (SHN-3191, Master SKU 10471-3191) that a run didn't catch, despite its "Rework Alert Sent" checkbox being unchecked and its WIP2027 Planning Indicator already reading "Dropped". Investigation via live Monday data (`get_board_info`) showed the Wholesale WIP board has grown to **338 items** (a large batch was bulk-created 2026-08-20), while `getWholesaleWipItems()` only ever fetched a single un-paginated `items_page(limit: 100)` - silently missing every item beyond whatever first 100 Monday happened to return, every single run, regardless of drop status. `get_board_activity` on the missed item confirmed it was never touched by the automation (only by Shelly directly and the board-relation auto-link), ruling out a processing failure - it simply never existed in this script's fetched item list.
    - Fixed `getWholesaleWipItems()` to page through the full board using `items_page`'s `cursor` (loop with `next_items_page(limit: 100, cursor: ...)` until the cursor is null).
    - Found and fixed the same class of bug in two more places while checking: `getPlanningIndicators()` and `getWip2027ImageExportState()` both pass a full list of item IDs to Monday's root-level `items(ids: [ID!])` query in one call - which is hard-capped at 100 IDs per call (confirmed via Monday's own API changelog: "You cannot return more than 100 items per query"). With 338 items on the board, `candidates`/`targets` lists can now exceed 100, so both were at real risk of the same silent-miss failure mode. Both are now batched into chunks of 100 via a new `chunk()` helper.
    - **Lesson for next time this board grows further**: none of these limits scale automatically - if the board keeps growing, watch for the same pattern anywhere a Monday query takes a bounded list of IDs or a single page without a cursor loop.
    - **Second, more severe bug in the same area, found the same day after the chunking fix above still didn't catch SHN-3191**: chunking to batches of 100 wasn't enough on its own, because `items(ids: $itemIds)` was called with no `limit` argument at all. Monday's API defaults that query's page size to **25** when `limit` is omitted - "hard-capped at 100" only describes the *maximum* you can request, not what you get if you don't ask. So every 100-ID batch was silently truncated to its first 25 items (in Monday's own internal ID ordering, unrelated to anything meaningful here), and the remaining ~75 items in each batch were never even queried - no error, no log line, `indicators[wip2027Id]` was just `undefined` and silently failed the `=== 'Dropped'` check. Confirmed directly against live Monday data: re-running the exact 100-ID batch containing SHN-3191's WIP2027 item (`11822756307`) without `limit` returned only 25 items and excluded it; adding `limit: 100` to the same call returned all 100, correctly showing `"Dropped"`. Fixed by adding `limit: 100` to both `items(ids:)` calls (`getPlanningIndicators()` and `getWip2027ImageExportState()`) - the single-ID lookup in `getMondayItemState()` (checking a prior pulse's state before appending vs. creating new) wasn't touched since it only ever requests one ID at a time, where the default of 25 can't matter.
    - **Full audit of every other Monday query in this script for the same missing-`limit` failure mode** (done 2026-08-21, prompted by Shelly asking "is there any other place this limit may be an issue"), checked against Monday's own schema via introspection rather than assumed:
      - `getWholesaleWipItems()`'s `items_page`/`next_items_page` - both already pass `limit: 100` explicitly (and Monday's schema marks `limit` as a *required*, non-null argument on `next_items_page`, so this couldn't have silently regressed the same way).
      - `getMondayUserByEmail()`'s `users(emails: $emails)` - takes a `limit` argument too (confirmed via schema, so the same class of bug is theoretically possible), but this is only ever called with a single email at a time (one sales rep lookup per order), never a batch - the default page size can't matter when you only ever ask for 1 result.
      - `getPulseState()`'s `items(ids: [$itemId])` - single ID, same reasoning as above, unaffected regardless of default.
      - `getAssets()` in `lib/monday.js` (resolves WIP2027 image file assets for the Dropbox export feature) - checked whether the same fix applied here too. It does NOT: Monday's schema shows `assets(ids:)` has **no `limit` argument at all** (unlike `items`/`users`/`next_items_page`), so `assets(ids: $ids, limit: 100)` would just be a GraphQL schema error, not a fix. Also moot in practice either way, since this is only ever called with one Wholesale WIP item's own image files at a time (comment on `exportImagesToDropbox` notes ~5 images is the practical max per item) - never a large batch.
      - All remaining `mondayGraphQL()` calls in `run.js` (`setAddToNuOrderToRemove`, `postComment`, `createOrUpdatePulse`, `markReworkAlertSent`, `createSalesOpsCsvPulse`, `markImagesExported`, `setAddToNuOrderToUpdateNetSuite`) are mutations or single-item lookups - no plural ID-list query involved, not exposed to this failure mode at all.
      - NetSuite's `runSuiteQL()` (in `lib/netsuite.js`) already does real `limit`/`offset` pagination with an explicit `hasMore` loop - unrelated bug class, already correct, not touched.
      - Conclusion: the two fixes above (`getPlanningIndicators()`, `getWip2027ImageExportState()`) were the only two places this bug actually existed. Nothing else in the workflow needed a change.

23. **Board growth limits and error-alerting coverage reviewed 2026-08-21 (same day as decision 22, prompted directly by that incident) - no code changes made, documented here so this isn't re-litigated from scratch later.**
    - **Monday item-limit numbers, confirmed via monday's own docs (Shelly had asked about a "500 or 1000" limit)**: most plans allow up to **10,000 items per board** (Enterprise: 100,000) before monday blocks new item creation with its own on-screen banner - Wholesale WIP is at 338, nowhere close. The 500/1000 figures were likely a mix-up with two unrelated numbers: `items_page`/`next_items_page`'s `limit` argument maxes out at **500** (this script uses 100, which just means more page round-trips, not a correctness limit - could be raised to 500 as a minor efficiency tweak, not required), and the root `items(ids:)` query's `limit`/`ids` both cap at **100** (already matched exactly by decision 22's fix). None of this needs a code change unless the board grows roughly 30x from where it is today.
    - **Error-alerting coverage - what's real today**: every per-item/per-order operation is wrapped in its own try/catch that posts a non-fatal Slack alert and lets the run continue (NetSuite lookups, Monday writes, Celigo trigger, imagery export). A top-level `main().catch(...)` at the bottom of `run.js` catches anything that escapes those, posts a `fatal: true` Slack alert, and exits non-zero (which also fails the GitHub Actions step).
    - **Three real gaps identified, explicitly left as-is per Shelly ("lets leave it as is for now")**:
      1. `lib/monday.js`/`lib/netsuite.js`/`lib/dropbox.js` validate their required env vars at **module-load time** (by design, since they're true hard dependencies - unlike Celigo, see decision 21). If one of those secrets is ever missing/revoked, the crash happens while `run.js` is still being imported, *before* the top-level catch handler in `run.js` even exists yet - so no Slack alert fires, only a failed GitHub Actions run.
      2. Infrastructure-level failures (runner won't start, `actions/checkout`/`actions/setup-node` fail, or the job hangs past GitHub's own timeout) never reach the Node script at all, so there's no way for in-script Slack alerting to catch them. GitHub Actions does have its own opt-in failure-notification email for scheduled workflows (sent to whoever's GitHub notification settings have Actions failures enabled, attributed to whoever pushed/owns the workflow) - Shelly was pointed at this as a free, low-effort partial mitigation for gaps 1 and 2, but has not confirmed whether she has it turned on.
      3. **The one that actually caused decision 22's incident, and the one no watchdog-style "did the run succeed" check would ever catch**: a run can complete cleanly, log `Done.`, and still be silently wrong, because the underlying bug (Monday quietly returning less data than asked for) never throws anything. The real mitigation for this category isn't monitoring run success/failure - it's an in-script sanity check (e.g., alert if a board's fetched item count drops suspiciously vs. a rolling baseline), which was proposed but **not built** - Shelly chose to leave this as-is for now rather than add it.
    - **If this comes up again**: don't propose a generic "watchdog workflow" as the fix for gap 3 - it wouldn't have caught either of decision 22's bugs, since both runs reported success. A data-sanity check inside the existing run is the only thing that actually addresses that failure mode.

24. **"No images found" on feature 3's export now treated as a real export failure, not a silent skip** (changed 2026-09-08, Shelly's request after reviewing the flow diagram): previously, `processNuOrderImageExports()` had four skip conditions that all `continue`d silently (leaving "Add to NuOrder" untouched, retried next run) - no WIP2027 match, already exported, no Master SKU, and **no images in WIP2027's Image column**. Shelly asked specifically that the fourth one stop being a silent skip: "if the image is not present in the image column, it should NOT skip the run, it should instead attempt export then fail the export and send a slack alert, then continue to the flip 'Add to NuOrder' step." The other three skip conditions are unchanged and still `continue` as before - this only applies to the empty-Image-column case.
    - **Implementation**: the `state.files.length === 0` check and its `continue` were removed from `processNuOrderImageExports()`, so an empty Image column now falls through into the existing `try { await exportImagesToDropbox(...) }` block like any other target. `exportImagesToDropbox()` itself now throws `new Error('No images found on linked WIP2027 item')` as its very first check when `files.length === 0`.
    - **Why the throw had to be added inside `exportImagesToDropbox()` rather than just deleting the skip**: `exportImagesToDropbox()`'s upload loop is a plain `for (let i = 0; i < files.length; i++)` over the `files` array - with zero files, that loop simply never executes and the function returns normally with zero uploads. Without the explicit throw, removing the skip would have produced a **false success**: `markImagesExported()` would still fire, permanently marking the item as "exported" with nothing actually in Dropbox, and no alert would ever fire. The throw is what makes this behave like every other export failure.
    - **Resulting behavior**: no images -> `exportImagesToDropbox()` throws -> caught by the existing `catch` block -> logs + `sendSlackAlert()` with a clear "No images found on linked WIP2027 item" message -> the existing `finally` block still runs unconditionally, flipping "Add to NuOrder" to "Update NetSuite" exactly like a successful export or any other kind of failure (per decision 20a - Shelly's original "fire regardless" call). The item still drops out of the `targets` filter on the next run either way, so this doesn't cause repeated re-alerting.

25. **Split the single combined script/workflow into three separate GitHub Actions workflows, same repo** (2026-09-08, Shelly's request: "break this into a couple different workflows but keep them in the same repo... still want them to trigger off of each other but also want to do a component on its own"). The original `scripts/run.js` + `.github/workflows/daily-check.yml` combined four things in one process every run: the Dropped trigger (base automation + Feature 1 + Feature 2), Feature 3 (NuOrder imagery export, already documented as "wholly independent" of the Dropped trigger - decision 19), and the end-of-run Celigo sync trigger (decision 21). Split along those natural seams:
    - **`scripts/dropped-alert.js`** / **`.github/workflows/dropped-alert.yml`** ("Dropped Rework Alert"): Planning Indicator -> Dropped detection, Feature 1 (Add to NuOrder -> Remove), the NetSuite order match -> Wholesale L10 pulse/comment, Feature 2 (Sales Ops L10 close-items CSV), and the "Rework Alert Sent" checkbox. Owns `state/state.json`. Same twice-daily cron (02:00/03:00 UTC) + in-script 8pm-Mountain gate + `workflow_dispatch` as before.
    - **`scripts/nuorder-imagery-export.js`** / **`.github/workflows/nuorder-imagery-export.yml`** ("NuOrder Imagery Export"): Feature 3 only - export images to Dropbox for linked items whose "NuOrder Images Exported" checkbox is unchecked (trigger changed the same day - see decision 26 below, this is no longer gated on "Add to NuOrder"). Same cron/gate/`workflow_dispatch` pattern. This also happens to deliver, at least the GitHub-Actions half of it, the standalone "run imagery export only" ability that was scoped for a Slack manual trigger back in 2026-08-08 and paused (see the PAUSED section in project memory) - it's now just a manual Actions run instead of a Slack command.
    - **`scripts/celigo-sync.js`** / **`.github/workflows/celigo-sync.yml`** ("Celigo NetSuite Sync"): just the `runCeligoFlow()` call. Both of the other two workflows can change "Add to NuOrder" (Remove from Feature 1, Update NetSuite from Feature 3), so both need Celigo to run afterward - rather than duplicate the trigger call in two places, it lives here once. No `schedule:` of its own - triggered by **`workflow_run`** listening for either "Dropped Rework Alert" or "NuOrder Imagery Export" to complete (GitHub Actions supports naming multiple source workflows in one `workflow_run.workflows:` list), plus its own `workflow_dispatch` for on-demand use independent of the other two.
    - **Shared code, split out of the monolith into `scripts/lib/`**: `wholesale-wip.js` (the paginated `getWholesaleWipItems()` fetch, `chunk()`, `isChecked()`, and the Wholesale WIP board/column constants both remaining scripts need) and `schedule.js` (`isManualRun()` / `isEightPmMountain()`, the 8pm-Mountain-tick gate used by all three). `lib/monday.js`, `lib/netsuite.js`, `lib/dropbox.js`, `lib/celigo.js` are unchanged and each imported only by the script(s) that actually need them - e.g. `celigo-sync.js` never touches Monday or NetSuite secrets at all now, `dropped-alert.js` never touches Dropbox.
    - **`lib/slack.js`'s `sendSlackAlert()`** gained a `source` option (defaults to the old combined workflow's name for safety) so alerts posted to #whsl_ops_workflow_alerts say which of the three workflows sent them - previously every alert said "Wholesale Drops Order Alert workflow" regardless of which feature failed, which would've been confusing to debug once split three ways.
    - **Two explicit tradeoffs confirmed with Shelly before building, don't relitigate without reading this**:
      1. Both `dropped-alert.js` and `nuorder-imagery-export.js` independently call the same paginated `getWholesaleWipItems()` fetch when they run around the same time - i.e. the whole board gets read twice instead of once. Confirmed fine with Shelly ("one cheap paginated read per workflow run, not per item").
      2. On a normal day, Celigo now fires up to **twice** (once after Dropped Rework Alert's real run, once after NuOrder Imagery Export's real run) instead of once like the old combined script. Confirmed fine with Shelly ("always, even on failure" - matches the original "fire regardless" design intent, just per-workflow now instead of per-run). `celigo-sync.js` applies the same 8pm-Mountain gate as the other two so it does NOT also fire on the twice-daily cron's throwaway DST tick (workflow_run fires on every upstream completion regardless of what that run actually did, so without this gate it would fire 4x/day instead of up to 2x/day).
    - `scripts/run.js` and `.github/workflows/daily-check.yml` were removed (git rm) - fully superseded by the three files above.

26. **NuOrder Imagery Export's trigger changed to be purely the "NuOrder Images Exported" checkbox - no longer gated on or tied to "Add to NuOrder" at all** (2026-09-08, same day as decision 25, Shelly's follow-up request): "change the trigger to the Nuorder images exported [checkbox] column. If checked, do nothing, if unchecked, run the code to export then check the column when completed w/o error. do not change the 'add to Nuorder' status." Two things changed together:
    - **Target scope**: `processNuOrderImageExports()`'s `targets` filter used to require `addToNuOrder === 'Add'`. That's gone - every Wholesale WIP item with a linked WIP2027 item is now a candidate, full stop. Confirmed with Shelly before building: this makes the feature a standing "keep Dropbox in sync with whatever images exist" process for every linked item, independent of where that item sits in the NuOrder pipeline, rather than something that only runs once someone manually flips a status to "Add".
    - **No more status flip**: the end-of-attempt flip to "Add to NuOrder" = "Update NetSuite" (decision 20a) is removed entirely, per Shelly's explicit "do not change the Add to NuOrder status." There's no `finally` block anymore - just try/catch. A failed export (including "no images found," decision 24) simply leaves the checkbox unchecked and gets retried on the next run, same as any of the other skip conditions - no separate retry bookkeeping needed.
    - **Column clarification worth remembering**: Shelly referred to the trigger column by Wholesale WIP's mirror, `lookup_mm613cyz` - but mirror/lookup-type columns can't be read via Monday's API at all (the same platform limitation documented for Planning Indicator - see "Platform quirks" below). Confirmed with her before building that the real underlying field is WIP2027's own `boolean_mm61mp3r` ("NuOrder Images Exported") - the same column this script already read and wrote before this change, so no new field access was needed, just a change in what gates the target list and what happens (or doesn't) afterward.
    - Since "Add to NuOrder" is no longer read or written by this workflow at all, `WHOLESALE_WIP_BOARD`/`COL_ADD_TO_NUORDER` were dropped from its imports.

27. **Celigo NetSuite Sync workflow now triggers a second Celigo flow** (2026-09-08, same day, Shelly's request): "add an additional step after the celigo netsuite sync... trigger an additional celigo flow NetSuite Item to NuOrder Product & Size Create/Update." `scripts/celigo-sync.js` now calls `runCeligoFlow()` twice in sequence - the existing "Monday to NetSuite" flow (`6a7520cb8d2dd2212253f91d`), then the new **"NetSuite Item to NuOrder Product & Size Create/Update"** flow (`66e07296960e3ef4199b3c02`, same integration `66f5896552a1b5dd437da348`, different flow builder id - from `https://integrator.io/integrations/66f5896552a1b5dd437da348/flowBuilder/66e07296960e3ef4199b3c02`). Each call is wrapped in its own independent try/catch (same pattern used everywhere else in this codebase) so a failure in one doesn't block or get masked by the other - both are attempted every time this workflow actually runs (i.e., past the 8pm-Mountain gate), regardless of whether the first one succeeded.

28. **Order matching switched from "shipped" to "allocated" quantity, plus an explicit closed-line exclusion** (2026-09-10, Shelly's request): `findMatchingOrderLines()`'s match condition used to be "ordered quantity > shipped quantity" (shipped computed via `PreviousTransactionLineLink`/`ShipRcpt`, see decision 6) - a line only dropped off the rework-alert list once it had actually shipped. Shelly asked for this to instead compare ordered quantity against NetSuite's own **Allocated** quantity (`transactionline.quantityallocated`, the standard Order Allocation field) - inventory can be reserved/committed to a line well before it physically ships, so a fully-allocated line no longer needs a rework alert even though nothing's shipped yet.
    - **Verified against real, live data before building** (Master SKU 80070-2419, 2026-09-10): at the time of checking, nothing had shipped yet on any of that SKU's 7 open wholesale orders, so the *old* shipped-based logic would have flagged all 7 in full. Under the *new* allocated-based logic, only 3 of the 7 still qualify (NuO315686018, WSO80614_999, WSO80629_998) - the other 4 (NuO310651618, NuO316139654, NuO316372106, NuO314493039-1) had every line already fully allocated and dropped out entirely.
    - **Closed-line exclusion added at the same time**: while verifying the above, found that NuO315686018 had 4 of its 5 lines already marked `isclosed = 'T'` in NetSuite despite showing 0 allocated - meaning the raw "ordered > allocated" comparison alone would have resurfaced lines NetSuite itself already treats as resolved. Shelly confirmed closed lines should never trigger a rework alert regardless of their allocation, so `findMatchingOrderLines()`'s SQL now also filters `sol.isclosed = 'F'`. In that same example, this correctly leaves NuO315686018 flagged (its one open, unallocated Small line still qualifies) while dropping the other 4 already-closed lines from consideration.
    - **Implementation**: the `LEFT JOIN` to a `PreviousTransactionLineLink`/`ShipRcpt` subquery and the `qty_shipped` column are gone; `transactionline.quantityallocated` (aliased `qty_allocated`) is selected directly off `sol` with no extra join needed, and `sol.isclosed = 'F'` was added to the `WHERE` clause alongside the existing match condition.
    - `docs/spec.md` decision 6 ("not fulfilled, line level") is superseded by this decision for the matching logic itself, though the header-level "not fulfilled" statuses (B/D/E, decision 5) are unchanged.

29. **Planning Indicator trigger moved off WIP2027 entirely - now reads Wholesale WIP's own native "WHSL Planning Indicator" column directly** (2026-09-23, Shelly's request): decision 1's original design read WIP2027's real `status_1__1` field via the `board_relation_mm5z9qmh` link, specifically because Wholesale WIP's own Planning Indicator was a `lookup`/mirror column type and monday's API cannot read those at all (see "Platform quirks"). Shelly added a genuine, non-mirror status column directly to Wholesale WIP - **"WHSL Planning Indicator"** (`color_mm79m1dk`) - which sidesteps that limitation entirely, so the trigger no longer needs to go through WIP2027/the board-relation link at all. `getWholesaleWipItems()` in `lib/wholesale-wip.js` now fetches `color_mm79m1dk` directly alongside its other columns; the old `WIP2027_BOARD`/`COL_PLANNING_INDICATOR = 'status_1__1'` constants and the `getPlanningIndicators()` batched-fetch helper (the one decision 22 fixed the `items(ids:)` default-limit-25 bug in) were removed from `dropped-alert.js`. Detection logic itself is unchanged - `droppedItems = candidates.filter(c => c.planningIndicator === 'Dropped')` - same value, same "Rework Alert Sent" idempotency semantics, just sourced from a different column read in a single existing paginated fetch instead of a second batched `items(ids:)` call. Committed as `4a4bfbe`.
    - **Note for later**: `WIP2027_BOARD`/`COL_WIP2027_IMAGE` constants come back into `dropped-alert.js` later the same day (decision 30 below) for a completely unrelated reason - WIP2027 is still the only place a real, readable Image field exists, Wholesale WIP's own Image column is still a read-only mirror. Don't assume WIP2027 involvement is fully gone from this file just because the Planning Indicator dependency on it is.

30. **Feature 3: dropped-styles digest - built as an email, blocked by a Google Workspace org policy, pivoted to a Slack post the same day** (2026-09-23, Shelly's request: "I want to an email created that will send 1 email to all employees that have the is inside rep (custentity_shin_inside_rep) field selected in NetSuite... send to their email on the customer record... contain a list of the items dropped in the run. Sort them by body type with a header and detail the master SKU and Print title. Include the image if available in the wholesale WIP"). Confirmed several specifics with Shelly via direct questions rather than assuming: broadcast to the whole "Is Inside Rep" group (not per-order targeting), each employee's own standard `email` field on their Employee record (not the Customer record, despite Shelly's initial phrasing), "body type" grouping = Product Type sourced from Wholesale WIP's own `text_mm4mg5an` column, Print Title from Wholesale WIP's own `text_mm4mef1k` column (both native text columns, not mirrors). Unconditional side effect of a drop, same timing as Feature 1 - fires for every newly-Dropped item regardless of whether it also matched a NetSuite order, once per RUN rather than per item.
    - **First built as an email** (`getInsideRepEmails()` via SuiteQL against `employee` where `custentity_shin_inside_rep = 'T'`, HTML digest built by `buildDropDigestHtml()`, sent via `lib/email.js` using `nodemailer` over Gmail SMTP with an app password). A mockup (grouped by Product Type header, sorted alphabetically within a group by Print Title, real photo embedded for one sample item) was built and approved before wiring up real sending.
    - **Killed by a real, confirmed-unfixable blocker**: the first live test run failed with `535-5.7.8 Username and Password not accepted` from Gmail SMTP. Diagnosed as a Google Workspace org-wide policy (Admin console -> Security -> Authentication) blocking App Password sign-in for *any* mailbox in the org, not something specific to the mailbox first tried - confirmed by Shelly directly ("i cannot set it up so that we can sign in w an app password in google") when asked whether switching to a different shared mailbox (`wholesale@shinesty.com`) would help; it wouldn't, since the block is org-wide, not per-mailbox.
    - **Final decision: skip email entirely, post to Slack instead** - the exact same content, delivered as a Block Kit message to **#wholesale-drops** (channel id `C0C42NW52QH`) via a **new, separate** Slack Incoming Webhook (`WHOLESALE_DROPS_SLACK_WEBHOOK_URL`). Slack Incoming Webhooks are bound to one fixed channel at creation time - this couldn't reuse the existing error-alerts webhook (`SLACK_ALERTS_WEBHOOK_URL`, posts to `#whsl_ops_workflow_alerts`), a different destination channel needs its own webhook URL/secret, not a runtime parameter.
    - **Implementation** (`lib/slack.js` `sendSlackBlocks()`, `dropped-alert.js` `buildDropDigestSlackBlocks()`/`sendDropDigestToSlack()`): a bold `section` block header per Product Type (alphabetical), then one `section` block per item within that group (alphabetical by Print Title) with `*Master SKU*\nPrint Title` as the text and an `accessory` image block when `getDropImageUrl()` finds one on the linked WIP2027 item's real Image column (`files2__1`) - unlike the email version, this only needs the presigned `public_url` since Slack fetches the image itself, no download/re-upload/attachment needed. Chunked at 49 blocks per Slack post (Slack's hard cap is 50 blocks/message) - only the first chunk gets a `header` block so a large drop run still reads as one contiguous list in the channel even when split across multiple posts.
    - `nodemailer` and `lib/email.js` were removed entirely once the pivot was made - the repo is back to having zero npm dependencies (no `package-lock.json` needed either).

31. **Feature 4: "Remove" pulse on the Wholesale Catalog/NuOrder L10 board, with a due-date rule added the same day** (2026-09-23, Shelly's request, due-date rule added as an immediate follow-up: "set the due date to 2 days from the pulse creation excluding weekends. If the date would fall on a saturday, assign it to Friday and assign to Monday for anything Sunday"). Same unconditional per-drop timing as Feature 1/3. One pulse per dropped Wholesale WIP item, created on board `18381819120` ("Wholesale Catalog/NuOrder L10") under group **"Catalog Changes/Updates"** (`group_mkxssj5e`), named in the exact format Shelly specified: `` Remove {Product Type} - {Print Title} - {Master SKU} `` (falling back to `(no product type)`/`(no print title)` placeholders rather than blank text when either is missing).
    - **Due Date** (`date4` column) computed by `computeCatalogRemovalDueDate()`: today + 2 calendar days, computed in **Mountain time** via `Intl.DateTimeFormat`/`formatToParts` (not the GitHub Actions runner's UTC clock) to match every other date-sensitive part of this workflow, then adjusted - a Saturday landing moves back one day to Friday, a Sunday landing moves forward one day to Monday.

32. **Duplicate-pulse guard added for the Catalog removal and Sales Ops L10 pulses - and a same-day bug where an earlier commit claimed to add this but didn't actually wire it in** (2026-09-23, Shelly's request after a live test run: "for the monday pulse creation in the Wholesale Catalog/nuoder l10 board and the sales Ops L10 board, update the workflow so that it looks if the pulse has already been created and does not create a duplicate. i cannot set it up so that we can sign in w an app password in google" - the second sentence there is unrelated, about the email blocker above, not this feature). Guards against a partial failure on an earlier run - e.g. the "Rework Alert Sent" checkbox update failing after a pulse already got created - leaving an item looking unprocessed and re-creating the pulse (and, for the CSV pulse, re-uploading a duplicate close-items CSV) on the next run.
    - Added `findItemByExactName(boardId, name)` to `lib/monday.js`: Monday's `items_page` `query_params` only supports `contains_text`/`not_contains_text` on the `name` column (confirmed via `get_column_type_info` - there is no true exact-match operator), so this fetches the small set of substring matches (`limit: 25`) and filters down to an exact string match itself. The GraphQL variable for `compare_value` has to be declared `CompareValue!`, not `String!` - the wrong type produces a real GraphQL variable-type error ("used in position expecting type 'CompareValue!'"), not a value-level one.
    - **Bug found the same day**: the first attempt at this guard was committed as `089a74d` with a commit message describing the fix as complete ("Skip creating duplicate Catalog/NuOrder L10 and Sales Ops L10 pulses on re-runs") and did add `findItemByExactName` to `lib/monday.js` - but never actually called it from `createCatalogRemovalPulse()` or `createSalesOpsCsvPulse()` in `dropped-alert.js`. The guard was documented as done but didn't exist in practice. Found and fixed later the same day while doing the unrelated Feature 3 Slack pivot above - both functions now call `findItemByExactName` up front and skip creation (for the Sales Ops CSV pulse, returning the existing item's id instead of a new one) when a match is found. **Lesson**: a commit message describing a fix is not itself verification that the fix is in the diff - worth actually re-reading `git show <commit> -- <file>` when a specific guard/behavior needs confirming, not just trusting a summary of what an earlier session built.
    - Also surfaced two real duplicate pulses already sitting on the Catalog board from before this guard existed (`"Remove Thongs - Modal - The Thoughts On Slots - 10398-2929"`, ids `13120066356` and `13120352156`). Per the standing policy of never deleting records directly, these were flagged to Shelly rather than removed - she still needs to delete whichever one she doesn't want herself if she wants them cleaned up.

33. **A commit that silently didn't contain its own described changes, caught by a live run failure** (2026-09-23): the commit meant to land the Feature 3 Slack pivot (`bcf3e88`) updated every other changed file correctly (`lib/slack.js`, `package.json`, `package-lock.json`'s removal, the workflow yml, `lib/email.js`'s deletion) but `scripts/dropped-alert.js` itself ended up committed with its OLD email-based content still in place - still importing the by-then-deleted `lib/email.js`. The file's content and `node --check` output were both re-verified as correct immediately after writing it and again right before staging it, but the actual committed blob was still the old version - root cause not fully pinned down.
    - **Symptom**: the next live GitHub Actions run failed immediately with `Cannot find module '.../lib/email.js'` (`ERR_MODULE_NOT_FOUND`), since `email.js` had already been deleted in that same commit.
    - **Fix**: rewrote `scripts/dropped-alert.js` directly via a `device_bash` heredoc (rather than the `device_commit_files` path used for the first attempt) and, this time, verified the actual committed content with `git show HEAD:scripts/dropped-alert.js` immediately after committing - before telling Shelly to push - rather than trusting `git status`/`node --check` against the working tree, neither of which proves what actually got committed. Fixed commit: `1815e13`. Confirmed working after Shelly pushed and re-ran the workflow.
    - **Lesson for next time a commit's correctness matters** (e.g. right before asking Shelly to push and test): verify with `git show <commit>:<path>` - the actual committed blob - not just the working tree state or a syntax check, both of which can look fine while the commit itself doesn't match.

## Platform quirks discovered along the way (useful if this ever breaks)

- monday.com's GraphQL API flatly cannot read/filter `lookup` (mirror) column types - not a reliability quirk, a hard "not supported" error.
- NetSuite's SuiteQL metadata-catalog endpoint (`/services/rest/query/v1/suiteql/metadata-catalog/...`) 500s for `salesorder`, `item`, and `transaction` record types due to unrelated dangling custom field references (`custbody_nu_shipping_calc_error`, `custitem_shinesty_forecast_end_date`) somewhere in the account's customization. Worth a NetSuite admin cleaning up eventually; the record-level metadata endpoint (`ns_getRecordTypeMetadata` equivalent, REST record API) works fine and was used instead to confirm field names.
- The standard `class` field can't be referenced bare in SuiteQL in this account (parser errors) - `cseg_order_class` works fine. To resolve a custom segment's display label from its internal ID in SuiteQL, use `BUILTIN.DF(field_name)` - the segment's own table name isn't queryable directly.
- `quantityFulfilled` on sales order lines: exists on the record, but explicitly `NOT_EXPOSED - Not available for channel SEARCH`. Use the `PreviousTransactionLineLink` / `ShipRcpt` approach instead.
- `salesrep` (on salesorder) and any field on `customer` error out completely via bulk SuiteQL (generic "unexpected SuiteScript error"), but read fine via a single-record REST fetch (`/services/rest/record/v1/...`). The script uses SuiteQL only for the order/line matching query, then per-matched-order record fetches for rep resolution.
- `transactionline.quantity` is stored **negative** for outbound transactions (Sales Orders, Item Fulfillments) per NetSuite's internal sign convention - always `ABS()` it.
- NetSuite's REST record API has no generic `item` endpoint - `/record/v1/item/{id}` 404s with `"Record type 'item' does not exist."`. Each item subtype needs its own endpoint name (e.g. `inventoryitem` for InvtPart). The script maps `item.itemtype` (pulled in the main SuiteQL query) to the right endpoint name for the pulse-comment item lookups, defaulting to `inventoryitem` since that's what all Wholesale-relevant items have been so far.
- The custom "Order Class" segment field needed its own role-level permission grant beyond normal Transaction/Item access before the TBA integration role could query it: Setup > Users/Roles > Manage Roles > [role] > Permissions > **Custom Record** category needed an "Order Class" entry (View). Separately, and less obviously, the SuiteQL REST endpoint (`/query/v1/suiteql`) also required the role to have **SuiteAnalytics Workbook** permission (found under the **Reports** permission category) - without it, every SuiteQL call 400s with a generic `"Your current role does not have permission to perform this action"` regardless of which table/field is referenced. Both were missing on Shelly's TBA integration role and had to be added before the first real query succeeded.
- monday.com's `Mutation.create_update` field requires the `updates:write` OAuth scope on the app. Editing an app's scopes on developer.monday.com doesn't retroactively upgrade a token issued under an older/draft scope set - the fix that finally worked was deleting the app entirely and creating a new one with the correct scopes from the start, then doing a completely fresh authorize flow. If this error (`"Unauthorized to load field 'Mutation.create_update', Reason: missing required scopes."`) ever comes back, check the app's scopes first, but don't assume adding a missing scope to an existing app and re-authorizing is enough - it wasn't, for reasons not fully pinned down.
- **"Option B" for feature 3's Monday columns** (2026-08-08): the idempotency checkbox and re-trigger button for the imagery-export feature were first created directly on Wholesale WIP, then moved to WIP2027 instead. Reason: WIP2027 had no Connect-Boards column pointing back to Wholesale WIP (only the reverse link exists, `board_relation_mm5z9qmh`), so a native "button pressed on WIP2027 -> change column on Wholesale WIP" automation wasn't available without adding a whole new board-relation pairing. Since WIP2027 is also where the images actually live/get edited, both pieces were kept there instead: the button->uncheck-checkbox automation is a plain same-board automation, and the script resolves the linked WIP2027 item via Wholesale WIP's existing `board_relation_mm5z9qmh` and reads/writes WIP2027's own checkbox directly (a normal column read, not a mirror, so no API limitation). A read-only mirror of that checkbox (`lookup_mm613cyz`) was added back onto Wholesale WIP purely for human visibility - like all mirror columns on this account it is NOT readable via the API, so the script never reads it.
- monday's file-type column raw `value` is a JSON string shaped like `{"files":[{"name":"...", "assetId":123, "isImage":"true", "fileType":"ASSET", "createdAt":..., "createdBy":"..."}]}` - confirmed live against WIP2027's `files2__1` on both a single-file item and a real 2-file item (SHN-3232). The array order matches upload order (ascending `createdAt`), which is what feature 3 uses as the export sequence. Asset IDs from that JSON are resolved to a downloadable URL via the root-level `assets(ids: [...])` query, which returns a `public_url` (a presigned S3 link valid ~1 hour - fine since it's downloaded immediately in the same run) plus `file_extension` (returned WITH a leading dot, e.g. `".png"`).
- monday's file-upload mutations (`add_file_to_update`, `add_file_to_column`, etc.) can't be sent as normal JSON GraphQL requests - they require a multipart/form-data POST to `https://api.monday.com/v2/file` with the mutation as a `query` form field and the binary as a separate form field (e.g. `variables[file]`) referenced by name inside that mutation string. Node 20's built-in `FormData`/`Blob` make this possible with zero extra npm dependencies (see `lib/monday.js` `mondayUploadFile()`).
- Shelly's Dropbox account has **two** folders literally named "NuOrder Imagery" - the real one (`/NuOrder Imagery`, a top-level shared folder) and an unrelated leftover (`/Apps/nuorder-imagery-netsuite-sync/NuOrder Imagery`, nested under what looks like an app-scoped folder from an earlier, separate attempt at this same integration). Confirmed which one was correct by asking Shelly directly, since resolving her original share link's exact path via the Dropbox API would have required adding a `sharing.read` scope to the app - which, like the Monday OAuth scope issue above, likely would have meant redoing the whole authorize flow. `DROPBOX_IMAGERY_FOLDER` in `run.js` is deliberately the plain top-level path.
- NetSuite's REST record API only returns a transaction's sublist detail (item lines, with fields like `isClosed` and `lineUniqueKey`) when the request includes `?expandSubResources=true` - without it, sublists come back as link-only stubs. Confirmed live via `GET /record/v1/salesorder/{id}?expandSubResources=true` against a real order (MSO-4061): returns `item.items[]`, each shaped like `{ item: { id, refName }, itemType, line, lineUniqueKey, isClosed, quantity, ... }`.
- Slack Incoming Webhooks are bound to one fixed channel at creation time - there is no way to pick a different destination channel at send time. A second destination (this project's #wholesale-drops digest, alongside the original #whsl_ops_workflow_alerts error-alert webhook) needs its own separate webhook URL and its own secret, confirmed 2026-09-23 building decision 30's Slack pivot. Also: Slack rejects a single Block Kit message over 50 blocks - a large batch has to be chunked into multiple `chat.postMessage`-style webhook calls.
- Monday's `items_page`/`next_items_page` `query_params.rules` only supports `contains_text`/`not_contains_text` operators on the `name` column (confirmed via `get_column_type_info`, 2026-09-23) - there is no true exact-match operator. An exact-name lookup (see decision 32's `findItemByExactName`) has to fetch the substring matches and filter to an exact string match client-side. The GraphQL variable type for that column's `compare_value` is `CompareValue!`, not the more obvious `String!` - using the wrong type is a real GraphQL schema error ("used in position expecting type..."), not a runtime one.
